import { IGenericMessage } from "../utils";

export class PermissionsMessages {
}

export enum PermissionsConstants {
    PERMISSIONS_DISABLED = 'The channel owner has not enabled mod permissions.',
    PERMISSION_DENIED = 'Permission denied; you must be the channel owner or a channel mod.'
}